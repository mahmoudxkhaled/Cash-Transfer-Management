using My_Transfers.Models;
using Radzen;
namespace My_Transfers.Class;

public class Shared_Class
{


	//	public static readonly NotificationMessage MessageSucces =
	//new NotificationMessage { Severity = NotificationSeverity.Success, Summary = "Smart System", Detail = "Data Saved Successfully", Duration = 2000 };
	//	public static readonly NotificationMessage MessageDelete =
	//	new NotificationMessage { Severity = NotificationSeverity.Success, Summary = "Smart System", Detail = "Data Deleted Successfully", Duration = 4000 };

	//	public static NotificationMessage MessageError(string msg) =>
	//			new NotificationMessage { Severity = NotificationSeverity.Error, Summary = "Smart System", Detail = msg, Duration = 5000 };

	//	public async Task<bool> IsTransferCodeDuplicate(int transferCode , string ConStr)
	//	{

	//		List<Transfers> existTransfer = await RunSQL.RunSQLList<Transfers>($"SELECT TRANSFER_CODE FROM Transfers WHERE TRANSFER_CODE = '{transferCode}'", ConStr);

	//		// // int checkDuplicateQuery = existTransfer.Count();

	//		// if (existTransfer.FirstOrDefault().TRANSFER_CODE == null)
	//		if (existTransfer.Count > 0)
	//			if (existTransfer.FirstOrDefault().TRANSFER_CODE == null)

	//			{
	//				return false;
	//			}
	//			else
	//			{

	//				return true;

	//			}
	//		else return false;
	//	}

}







